# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
docs/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Ignore package tarball (built via "mix hex.build").
contract-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets/*
!/priv/static/assets/standard_contracts/
/priv/static/assets/standard_contracts/*
!/priv/static/assets/standard_contracts/employment_v1.editables.json

# 큰 바이너리 산출물(WASM 등)은 별도 repo의 빌드 결과라 contract 히스토리에
# 넣지 않는다.
*.wasm

# 루트 레벨 PNG 는 보통 dev 스크린샷 cruft. 정식 이미지는 docs/, assets/,
# priv/ 같은 정해진 디렉토리에 두고, 루트에 떨궈진 것은 추적 안 함.
/*.png

# Ignore digested assets cache.
/priv/static/cache_manifest.json

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/assets/node_modules/
/assets/vendor/

# App-local RHWP source/native runtime is provided by the ehwp dependency now.
# (ex_mcp is a remote git fork: github.com/humdrum00001010/ex_mcp — no longer vendored.)
/vendor/
/native/

# Native NIF build outputs.
/priv/native/

# Local environment file (secrets, never commit).
/.env
/.env.*

# macOS Finder metadata — local-only, never commit.
.DS_Store

# Playwright MCP scratch dirs and ad-hoc screenshot dumps at repo root.
/.playwright-mcp/
/landing-*.png
/dashboard-*.png
/studio-*.png

# Per-user agent harness state — local-only, contains scheduling/session data.
/.claude/
/.codex/
/.ecrits/
/priv/ecrits_repo/
/GLOBAL_GOAL.md

# Phoenix / app runtime logs — local-only, never commit.
/log/
*.log

# test artifacts some doc tests still write to CWD
/c.hwp
/save-by-path.hwp
/validate_inline_blank.hwp
